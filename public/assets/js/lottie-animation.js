var coworking_a = lottie.loadAnimation({
  container: document.getElementById('coworking-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/coworking-banner.json' // the path to the animation json
});
var coworking_b = lottie.loadAnimation({
  container: document.getElementById('coworking-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/coworking-anim-01.json' // the path to the animation json
});
var agency_a = lottie.loadAnimation({
  container: document.getElementById('agency-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/agency-banner.json' // the path to the animation json
});
var agency_b = lottie.loadAnimation({
  container: document.getElementById('agency-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/agency-anim-01.json' // the path to the animation json
});
var finance_a = lottie.loadAnimation({
  container: document.getElementById('finance-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/finance-banner.json' // the path to the animation json
});
var finance_b = lottie.loadAnimation({
  container: document.getElementById('finance-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/finance-anim-01.json' // the path to the animation json
});
var finance_c = lottie.loadAnimation({
  container: document.getElementById('finance-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/finance-anim-02.json' // the path to the animation json
});
var finance_d = lottie.loadAnimation({
  container: document.getElementById('finance-anim-03'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/finance-anim-03.json' // the path to the animation json
});
var seo_a = lottie.loadAnimation({
  container: document.getElementById('seo-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/seo-banner.json' // the path to the animation json
});
var seo_b = lottie.loadAnimation({
  container: document.getElementById('seo-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/seo-anim-01.json' // the path to the animation json
});
var seo_c = lottie.loadAnimation({
  container: document.getElementById('seo-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/seo-anim-02.json' // the path to the animation json
});
var social_media_a = lottie.loadAnimation({
  container: document.getElementById('social-media-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/social-media-banner.json' // the path to the animation json
});
var social_media_b = lottie.loadAnimation({
  container: document.getElementById('social-media-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/social-media-anim-01.json' // the path to the animation json
});
var social_media_c = lottie.loadAnimation({
  container: document.getElementById('social-media-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/social-media-anim-02.json' // the path to the animation json
});
var internet_a = lottie.loadAnimation({
  container: document.getElementById('internet-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/internet-banner.json' // the path to the animation json
});
var internet_b = lottie.loadAnimation({
  container: document.getElementById('internet-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/internet-anim-01.json' // the path to the animation json
});
var internet_c = lottie.loadAnimation({
  container: document.getElementById('internet-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/internet-anim-02.json' // the path to the animation json
});
var startup_a = lottie.loadAnimation({
  container: document.getElementById('startup-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/startup-banner.json' // the path to the animation json
});
var startup_b = lottie.loadAnimation({
  container: document.getElementById('startup-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/startup-anim-01.json' // the path to the animation json
});
var creative_a = lottie.loadAnimation({
  container: document.getElementById('creative-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/creative-banner.json' // the path to the animation json
});
var creative_b = lottie.loadAnimation({
  container: document.getElementById('creative-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/creative-anim-01.json' // the path to the animation json
});
var creative_c = lottie.loadAnimation({
  container: document.getElementById('creative-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/creative-anim-02.json' // the path to the animation json
});
var virtualreality_a = lottie.loadAnimation({
  container: document.getElementById('virtualreality-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/virtualreality-banner.json' // the path to the animation json
});
var virtualreality_b = lottie.loadAnimation({
  container: document.getElementById('virtualreality-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/virtualreality-anim-01.json' // the path to the animation json
});
var virtualreality_c = lottie.loadAnimation({
  container: document.getElementById('virtualreality-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/virtualreality-anim-02.json' // the path to the animation json
});
var business_a = lottie.loadAnimation({
  container: document.getElementById('business-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/business-banner.json' // the path to the animation json
});
var business_b = lottie.loadAnimation({
  container: document.getElementById('business-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/business-anim-01.json' // the path to the animation json
});
var business_c = lottie.loadAnimation({
  container: document.getElementById('business-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/business-anim-02.json' // the path to the animation json
});
var cyber_security_a = lottie.loadAnimation({
  container: document.getElementById('cyber-security-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/cyber-security-banner.json' // the path to the animation json
});
var cyber_security_b = lottie.loadAnimation({
  container: document.getElementById('cyber-security-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/cyber-security-anim-01.json' // the path to the animation json
});
var cyber_security_c = lottie.loadAnimation({
  container: document.getElementById('cyber-security-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/cyber-security-anim-02.json' // the path to the animation json
});
var cyber_security_d = lottie.loadAnimation({
  container: document.getElementById('cyber-security-anim-03'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/cyber-security-anim-03.json' // the path to the animation json
});
var fitness_a = lottie.loadAnimation({
  container: document.getElementById('fitness-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/fitness-banner.json' // the path to the animation json
});
var fitness_b = lottie.loadAnimation({
  container: document.getElementById('fitness-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/fitness-anim-01.json' // the path to the animation json
});
var fitness_c = lottie.loadAnimation({
  container: document.getElementById('fitness-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/fitness-anim-02.json' // the path to the animation json
});
var helpdesk_a = lottie.loadAnimation({
  container: document.getElementById('helpdesk-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-banner.json' // the path to the animation json
});
var helpdesk_b = lottie.loadAnimation({
  container: document.getElementById('helpdesk-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-anim-01.json' // the path to the animation json
});
var helpdesk_c = lottie.loadAnimation({
  container: document.getElementById('helpdesk-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-anim-02.json' // the path to the animation json
});
var helpdesk_d = lottie.loadAnimation({
  container: document.getElementById('helpdesk-anim-03'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-anim-03.json' // the path to the animation json
});
var helpdesk_e = lottie.loadAnimation({
  container: document.getElementById('helpdesk-anim-04'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-anim-04.json' // the path to the animation json
});
var helpdesk_f = lottie.loadAnimation({
  container: document.getElementById('helpdesk-anim-05'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/helpdesk-anim-05.json' // the path to the animation json
});
var hosting_a = lottie.loadAnimation({
  container: document.getElementById('hosting-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hosting-banner.json' // the path to the animation json
});
var hosting_b = lottie.loadAnimation({
  container: document.getElementById('hosting-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hosting-anim-01.json' // the path to the animation json
});
var hr_software_a = lottie.loadAnimation({
  container: document.getElementById('hr-software-banner'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hr-software-banner.json' // the path to the animation json
});
var hr_software_b = lottie.loadAnimation({
  container: document.getElementById('hr_software-anim-01'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hr_software-anim-01.json' // the path to the animation json
});
var hr_software_c = lottie.loadAnimation({
  container: document.getElementById('hr_software-anim-02'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hr_software-anim-02.json' // the path to the animation json
});
var hr_software_d = lottie.loadAnimation({
  container: document.getElementById('hr_software-anim-03'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hr_software-anim-03.json' // the path to the animation json
});
var hr_software_e = lottie.loadAnimation({
  container: document.getElementById('hr_software-anim-04'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/animation/hr_software-anim-04.json' // the path to the animation json
});
